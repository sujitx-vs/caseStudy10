apiVersion: v1
kind: Service

metadata:
  name: social-media-service

spec:
  type: NodePort

  selector:
    app: social-media-sentiment

  ports:
  - protocol: TCP
    port: 8888
    targetPort: 8888
    nodePort: 30080